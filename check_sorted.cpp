int isSorted(int n, vector<int> a) {
    for(int i= 0;i<a.size();i++){
        if(a[i]>=a[i-1]){

        }
        else{
            return false;
        }
    }
    return true;
        
        
}
